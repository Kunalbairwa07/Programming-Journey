/*
NULL Pointer = We assign null value to a pointer to show that it doesn't point to any Location.
*/
#include <iostream>
using namespace std;
int main()
{
    int *ptr = NULL;
    cout << ptr;
    return 0;
}