#include<iostream>
using namespace std;
int main(){
    int n;
    cout<<"Enter the number = ";
    cin>>n;
    for(int i=n; i>=1; i--){ 
        cout<<" "<<i<<endl;
    }
    return 0;
}

/*// In this firstly we inialize i=n that we enters,
than loop chck that it is greater than 1 than it follows the loop*/ 